<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="so.sabami.template.persistence.CookieDuplicationMapper">

  <select id="getCookieDuplicationByGrid" resultType="CookieDuplication">
    SELECT * FROM cookie_duplication WHERE grid = #{grid}
  </select>
</mapper>
